# 위키 로그

이 파일은 위키 작업 이력을 시간순으로 기록하는 추가 전용 로그다.

- 엔트리 형식: `## [YYYY-MM-DD] {ingest|query|lint} | {title}`
- 정렬: 오래된 엔트리가 위에 오고, 최신 엔트리가 아래에 온다.
- 추가 위치: 새 엔트리는 항상 파일 끝에 추가한다.
- 상세 규칙: [`docs/wiki/README.md`](README.md)를 따른다.

## [2026-05-22] query | 정적 웹 도구 프로젝트 방향성 기록

- 사용자 요청에 따라 이 저장소의 구현 방향을 현재 지식으로 기록했다.
- `docs/wiki/kb/concepts/static-web-tools-project.md`를 생성했다.
- `docs/wiki/index.md`를 생성하고 개념 항목을 추가했다.

## [2026-08-17] ingest | 정적 사이트 저장소·문서 운영 체계 이관

- 원본 저장소의 LLM 위키 운영 규칙을 정적 사이트에 맞춰 `docs/wiki/README.md`로 이관했다.
- 티켓·리뷰·커밋 문서와 GitHub 이슈/PR/CI 구성을 정적 사이트 검증 흐름에 맞게 추가했다.
- 기존 정적 웹 프로젝트 페이지에 필수 frontmatter와 내부 출처 형식을 적용했다.

## [2026-08-17] ingest | 비공개 신혼여행 초안과 구체화 절차

- 공개 `info/` 페이지에는 여행의 분위기, 큰 동선, 포르투 체류일과 취향 질문만 두는 첫 시각 초안을 만들었다.
- 실제 항공편·숙소·예약·일자별 시간·비용은 암호화 전까지 공개 저장소에 평문으로 남기지 않는 경계를 기록했다.
- 취향 인터뷰, 후보 비교, 비용 봉투, 비공개 발행 순서의 점진적 일정 구체화 절차를 `kb`에 기록했다.

## [2026-08-17] ingest | 여러 계획의 공개 인덱스와 개별 경로 분리

- `info/plan/`을 특정 여행의 문서가 아닌 여러 계획의 공개 인덱스로 정의했다.
- 이번 여행 초안은 고유 경로 `info/plan/iberian-passage-2026/`로 옮기고, 새 계획도 같은 경로 규칙으로 분리하도록 기록했다.

## [2026-08-17] ingest | 정적 사이트의 암호화된 일정 발행 흐름

- 비추적 `private/` 원본을 로컬 비밀 구문으로 AES-GCM 암호문으로 바꾸고, 계획별 페이지에서만 브라우저 복호화하는 흐름을 추가했다.
- 비밀 구문·평문·예약 정보가 저장소와 브라우저 영구 저장소에 남지 않는 운영 규칙과 암호화 왕복 검증을 기록했다.

## [2026-08-17] ingest | 공개 일정 초안의 정보 밀도 조정

- 개별 계획 페이지에 Day 1–15의 도시 흐름, 대략적 이동 시간, 항공권을 제외한 2인 예산 범위를 추가했다.
- 공개 초안에는 계획을 비교할 수 있는 범위 정보를 두되, 정확한 날짜·시각·예약·실제 비용은 암호화된 상세 계획에만 둔다는 경계를 보완했다.

## [2026-08-17] ingest | 짧은 비밀 구문 허용

- 사용자 요청에 따라 암호화 발행 스크립트의 최소 비밀 구문 길이를 4자로 낮췄다.
- 12자 미만의 구문은 추측에 약하다는 경고를 표시하고, 낮은 민감도의 공유에만 사용하도록 운영 기준을 기록했다.

## [2026-08-20] ingest | 보호된 HTML 일정 표 렌더링

- 암호화 전 원본에 HTML 조각을 사용할 수 있게 하고, 브라우저에서 허용한 HTML 태그와 링크만 렌더링하도록 했다.
- 세부 일정은 `rowspan`·`colspan`을 쓸 수 있는 표로 구성해 날짜·도시·숙박을 반복하지 않도록 했다.

## [2026-08-20] ingest | GitHub Pages 배포 워크플로

- `main` 푸시와 수동 실행에 대응하는 GitHub Pages 배포 워크플로를 추가했다.
- `private/` 평문 원본은 기존 Git·정적 사이트 검증 제외 규칙을 유지한다.

## [2026-09-09] ingest | 숙소 원문 요약과 한국 후기 균형

- 이전 세션의 수집 자료를 복구하고 원문 요약·개별 채택 경험·현재 비교표를 연결했다.
- 한국 후기 ≥ 전체 외국 후기 조건과 도시별 검증·HTML 갱신 절차를 기록했다.
- 보호된 현재 일정의 숙소와 보관 조건을 확인하고 상세 평문·복호화 구문은 위키에 남기지 않았다.

## [2026-09-09] ingest | 숙소 재열람과 자료 구조 통합

- 실패 원문의 일반 브라우저·동일 Reddit 게시글 ID 주소 재시도와 출처별 열람 이력을 기록했다.
- 원문 수·채택 경험 수·비교 숙소 수·전체 후보 수를 분리하고 권역과 숙소를 같은 행으로 구성했다.
- 현재 원장과 과거 기록 JSON의 목적·형식을 명시하고 기존 분할 파일을 내용 보존 후 통합했다.
- JSON 기반 정적 HTML 생성, 중복 역할 파일 방지, 데스크톱·모바일 확인 절차를 보강했다.
- 도시별 일정 조건·권역 우선 이유와 숙소별 장점·단점·레퍼런스를 분리해 표시했다.
- 허니문 비교에서는 도미토리형 숙소를 제외하고, 개인실 조건은 예약 전에 다시 확인하도록 했다.

## [2026-09-09] ingest | 숙소 권역의 실제 이동 경로 반영

- 보호된 일정의 공항·버스·열차 거점과 도시별 권역 대표점 사이 공개 도로 경로를 확인해, 캐리어 이동은 택시 기본 주행 시간과 차량 접근으로 분리해 판단했다.
- 권역 표에 추천순위와 실제 차량 경로를 표시하고, 해당 순서로 숙소 행을 정렬했다. 교통량·호출 대기·숙소 내부 이동은 기본 경로 시간에 포함하지 않는 한계를 함께 표시했다.
- 개인실 여부가 분명하지 않은 Granada Old Town Hostel을 현재 비교·채택 후기·후보 카탈로그에서 제외하고 원문 장부에만 보존했다.

## [2026-09-09] ingest | 실제 승하차 지점과 샌딩 투어 기준 정정

- 도시별 권역 표 앞에 한국어·현지어 지점 설명과 실제 승하차·캐리어 이동 표를 추가해, 도시 대표점과 버스·철도·공항 승강장을 구분했다.
- Porto의 TIC와 CP Campanhã 보행 연결, Lisbon IC 528의 Oriente 하차와 ALSA Oriente 출발, Sevilla 샌딩 투어의 Jardines de Murillo 집결·Granada Fuente de las Batallas 하차·캐리어 조건을 원문으로 확인해 순위에 반영했다.
- 사용자 특별 추가 숙소 링크는 공유 식별자를 보존하지 않고, 확인된 Barcelona 2인 전용 펜트하우스를 현재 비교에 넣으며 미확인·만료 링크는 결과만 원장에 남겼다.
